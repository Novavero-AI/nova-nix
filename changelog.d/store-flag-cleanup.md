- **eval accepts --store after the subcommand, and the test suite no longer needs a real platform store root.** The eval sub-parser rejected --store where build, push, and store delete accepted it, even though eval-side reads follow the selected store identically. The suite's IO eval harness now evaluates against a per-run temp store, so the store-writing eval tests run on every machine instead of skipping where /nix is absent or root-owned: the platform-store guard and its skips are gone, CI stops provisioning /nix on the Linux legs, and the symlink materialization tests now run on macOS and unprovisioned machines too.
