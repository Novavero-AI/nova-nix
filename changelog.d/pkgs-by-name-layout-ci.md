- **CI and the release smoke test follow the by-name layout's renamed paths.** `pkgs/windows/hello.nix` now builds the by-name `hello` package through the real stdenv rather than driving `cmd.exe` directly, so the E2E job's two hello builds collapse into one and its output check moves to `bin\hello.exe` (where `make install` actually puts it) instead of the output root. `publish.yml`'s archive smoke test now looks for `pkgs/windows/stdenv/default.nix` and `pkgs/windows/stdenv/setup.sh` at their moved paths.
